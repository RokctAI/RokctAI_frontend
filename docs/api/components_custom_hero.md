# API Reference: hero

Source file: `components/custom/hero.tsx`

## Whitelisted API Endpoints

### `function nextIndex(placeholderIndex + 1) % placeholders.length; setPlaceholderIndex(nextIndex); onPlaceholderChange?.(nextIndex); setIsTyping(true); }, 500); return ()`
*No documentation provided (generation failed).*

### `function Hero({ signupUrl = "/register", id, }: { signupUrl?: string; id?: string; })`
*No documentation provided (generation failed).*

### `function handleFilterChange(filter: FilterType)`
*No documentation provided (generation failed).*
