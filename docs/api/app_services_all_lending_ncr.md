# API Reference: ncr

Source file: `app/services/all/lending/ncr.ts`

## Classes

### class `NCRService`

## Whitelisted API Endpoints

### `function registration_number(session?.user as any)?.company?.license || "N/A"; const interestReport = await ReportService.getReport( "Loan Interest Report", filters, options, ); const interest_income_nca = interestReport.data?.reduce( (sum: number, row: any)`
*No documentation provided (generation failed).*

### `function bad_debt_write_offs(writeOffsResponse?.message || []).reduce( (sum: number, row: any)`
*No documentation provided (generation failed).*
