# API Reference: header

Source file: `components/custom/header.tsx`

## Whitelisted API Endpoints

### `function Header({ loginUrl = "/login", signupUrl = "/register", session = null, }: { loginUrl?: string; signupUrl?: string; session?: any; })`
*No documentation provided (generation failed).*

### `function renderBadge(id: number)`
*No documentation provided (generation failed).*
