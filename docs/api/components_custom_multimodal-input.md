# API Reference: multimodal-input

Source file: `components/custom/multimodal-input.tsx`

## Whitelisted API Endpoints

### `function MultimodalInput({ input, setInput, isLoading, stop, attachments, setAttachments, messages, append, handleSubmit, className, allowSuggestions, useWorker, forcedIntent, forcedAction, forcedEntity, models, selectedModelId, onModelChange, onLocalSubmit, }: { input: string; setInput: (value: string)`
*No documentation provided (generation failed).*

### `function nextIndex(pinIndex + 1) % pinOptions.length; setPinIndex(nextIndex); const selectedOption = pinOptions[nextIndex]; if (selectedOption === "Add a competitor here") { setActivePinContext("competitor"); if (input === "Check In" || input === "Check Out") setInput(""); setTimeout(()`
*No documentation provided (generation failed).*
