# API Reference: layout

Source file: `app/handson/layout.tsx`

## Whitelisted API Endpoints

### `function branding(await getGuestBranding()) as any; const userRoles = (session?.user as any)?.roles || []; const userRole = userRoles[0]; const isPaaS = (session?.user as any)?.isPaaS; const hasRole = (itemRoles?: string[])`
*No documentation provided (generation failed).*
