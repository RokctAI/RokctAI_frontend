# API Reference: queries

Source file: `db/queries.ts`

## Whitelisted API Endpoints

### `function createUser(email: string, password: string)`
*No documentation provided (generation failed).*

### `function saveChat({ id, messages, userId, }: { id: string; messages: any; userId: string; })`
*No documentation provided (generation failed).*

### `function deleteChatById({ id }: { id: string })`
*No documentation provided (generation failed).*

### `function deleteAllChatsByUserId({ userId }: { userId: string })`
*No documentation provided (generation failed).*

### `function getChatsByUserId({ id }: { id: string })`
*No documentation provided (generation failed).*

### `function getChatById({ id }: { id: string })`
*No documentation provided (generation failed).*

### `function createReservation({ id, userId, details, }: { id: string; userId: string; details: any; })`
*No documentation provided (generation failed).*

### `function getReservationById({ id }: { id: string })`
*No documentation provided (generation failed).*

### `function updateReservation({ id, hasCompletedPayment, }: { id: string; hasCompletedPayment: boolean; })`
*No documentation provided (generation failed).*

### `function getPendingReminders({ userId }: { userId: string })`
*No documentation provided (generation failed).*
