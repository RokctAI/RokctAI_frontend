# API Reference: app-sidebar

Source file: `components/custom/app-sidebar.tsx`

## Whitelisted API Endpoints

### `function AppSidebar({ ...props }: React.ComponentProps<typeof Sidebar>)`
*No documentation provided (generation failed).*

### `function roles(session?.user as any)?.roles || []; const modules = (session?.user as any)?.modules || []; const isClient = roles.some((r: string)`
*No documentation provided (generation failed).*
